name = "buck-fever-scores"
main = "worker.js"
compatibility_date = "2024-11-01"

# Create the namespace once with:
#   wrangler kv namespace create SCORES
# then paste the returned id below (replace PASTE_KV_NAMESPACE_ID).
[[kv_namespaces]]
binding = "SCORES"
id = "PASTE_KV_NAMESPACE_ID"
